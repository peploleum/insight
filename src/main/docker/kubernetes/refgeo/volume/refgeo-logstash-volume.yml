apiVersion: v1
kind: PersistentVolume
metadata:
  name: refgeo-storage-logstash-data
  namespace: refgeo
  labels:
    component: refgeo-logstash-volume
    role: data
    type: local
spec:
  capacity:
    storage: 20Mi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce
  storageClassName: local-storage 
  local:
    path: /home/capgemini/refgeo/binding/logstash/
  nodeAffinity:
    required:
      nodeSelectorTerms:
      - matchExpressions:
        - key: kubernetes.io/hostname
          operator: In
          values:
          - kubernetes-node
---
